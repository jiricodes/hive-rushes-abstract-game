#include "player.h"

void player_reset(t_player *player) {
	player->pos1.x = -1;
	player->pos1.y = -1;
	player->pos2.x = -1;
	player->pos2.y = -1;
}

int player_place(t_player *player, t_pos *pos) {
	
}